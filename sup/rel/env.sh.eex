#!/bin/sh

export RELEASE_COOKIE=sup-node # Maybe at some point add the subsystem, but we won't cluster with others anyway.

# This sorta works for AWS, where we get the internal IP
#export HOSTNAME=`curl http://169.254.169.254/latest/meta-data/local-ipv4`
# However, this seems to work on both AWS and GCP
export HOSTNAME=$(hostname -f)
export RELEASE_NODE="sup@${HOSTNAME}"
export RELEASE_DISTRIBUTION="name"
